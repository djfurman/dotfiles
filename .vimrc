"colorscheme badwolf "recommended by doug black
syntax enable "syntax highlighting is key
set backspace=indent,eol,start " keep the backspace key working as expected
set number " turn on line numbers
set tabstop=4  	"number of visual spaces per TAB
set softtabstop=4 "number of spaces in tab when editing
set expandtab  "send the tab character into spaces

